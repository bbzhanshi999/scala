package com.neuedu.chapter10

object Scala_coll_method3 {
  def main(args: Array[String]): Unit = {
   /* val list:List[Int] = List(1, 2, 3, 4)

    //fold折叠和reduce的区别在于会有外部参数参与计算
    //fold方法传递两部分参数，第一部分表示集合之外的数据
    //第二部分的参数表示数据的处理逻辑
    val i = list.fold(10)(_ + _)
    println(i)
    println(list.foldLeft(10)(_ - _))
    println(list.foldRight(10)(_ - _))

    list.foldLeft("10")((s,i)=>s+i)*/


  }
}

